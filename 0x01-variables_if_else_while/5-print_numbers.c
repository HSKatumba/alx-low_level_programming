#include <stdio.h>
#include <stdlib.h>

/**
 * main  entry point
 * description: numbers
 * return: always 0
 */

int main(void)
{	
	int num;

	for (num = 0; num < 11; num++)
	{
		printf("%d\n", num);
	}
	return (0);
}
